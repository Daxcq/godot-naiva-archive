# Godot 导入缓存与导出产物不入库
.godot/
build/

# Python 虚拟环境：路径绑死创建机器（pyvenv.cfg 里是绝对路径），复制到别的电脑无效。
# 换机后由 vision/setup_vision_env.py 自动重建，无需入库。
vision/.vision-venv/
vision/.vision-venv-BAK/
__pycache__/
*.pyc

# 临时诊断产物（不要提交）
_probe.log
_proc_check.txt
shot_*.png
shot_*.png.import
scripts/_probe*.gd
scripts/_probe*.gd.uid
scripts/diag_*.gd
scripts/diag_*.gd.uid
# 下划线开头的是临时/一次性脚本（截图、探针、调试），不要入库。
# 长期保留的回归自检都叫 check_*.gd，不受此规则影响。
scripts/_*.gd
scripts/_*.gd.uid
# 外部工具（如 opencode）生成的一次性截图/探针脚本，不要入库。
test_shot.gd
test_shot.gd.uid
test_shot.tscn

