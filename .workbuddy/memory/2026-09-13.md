# 2026-09-13

## 收口一条龙完成（2026-09-12~13，feat 全绿合 main + 打包 exe）
- 8 个 check_* 全绿：check_entrance_npc 2 FAIL 根因 = 模型改包装场景后按名找 "Model" 容器失效，改用 entry.model 断言（与 _process 同源）。
- 真窗口冒烟：--quit-after 600 + --log-file，exit 0 无 SCRIPT ERROR。
- **Windows GUI exe 的 stdout 不进 bash 管道**，所有自检输出必须 --log-file 落盘再 tail。
- 合 main 用零工作区操作：git push origin feat/entrance-npc-polish:main（ff）——并行会话把工作区搞乱时也能安全合并。
- ref 被删 7 次（嫌疑：GitHubDesktop 4 进程常开）；抢救流程：rev-parse <sha>^{commit} 全长 -> 写 .git/refs/heads/feat/entrance-npc-polish -> 立即 push -> ls-remote 验证。
- index.lock 残留 6.5 分钟且无 git.exe 进程 = 死锁可安全删（当时 GitHubDesktop 在跑）。
- 打包流水线（全 python subprocess，bash shim 的 PATH 已废 mkdir/sleep/grep 都没有）：
  1. 模板：GitHub release 下载 tpz(1.26GB) 只解 windows_{debug,release}_x86_64{,_console}.exe 4 件到 %APPDATA%\Godot\export_templates\4.6.3.stable\
  2. git archive <sha> 解 %TEMP%/naiva_build2（绕开被乱的工作区）-> --headless --import -> --export-release "Windows Desktop" build/NaivaArchive.exe
  3. **导出报「给定的导出路径不存在」= build/ 输出目录没建**（Godot 不自动建）。
- **pck 里 DirAccess.get_files() 列导入型资源带 .remap/.import 后缀** -> gallery_easel.gd 按 .jpg 过滤全落空 = 导出后画廊空白。修复：剥后缀+去重（gallery_easel.gd）+ include_filter="*.jpg,*.jpeg,*.png"（无空格！）。
- 产物 build/NaivaArchive.exe 142.7MB（嵌入 pck 单文件）；打包版视觉识别无摄像头自动回落鼠标模式=预期行为；exe 图标=preview.png。
- 最终分支状态：main = feat = 8c5956c（远端本地全对齐）。
- 注意：.workbuddy/memory 目录曾被并行会话清空（git status 出现 D），本文件为重建。

## 工作区二次损坏修复（2026-09-13 上午）
- 用户报"程序有报错"→ 根因：工作区又被大规模破坏（140 个跟踪文件从磁盘消失 + **archive_space.tscn 被清成 0 字节**），开编辑器/跑游戏必炸。
- 一键回血：`git checkout HEAD -- .` 恢复全部跟踪文件（untracked 的 opencode 新场景不受影响，保留）。
- opencode 两个完好装饰资产收编：scenes/mouse_hole.tscn（鼠鼠洞）、scenes/ticket_stand.tscn（售票站）——自包含纯程序几何零依赖，**未实例化进主场景**（挂载留待 opencode 继续/用户确认）。
- 恢复后回归：check_input / check_entrance_npc / check_interaction_map 全 PASS，真窗口冒烟 exit 0 零脚本错误。
- ref 第 8 次被删，同流程抢救。最终 main = feat = 4f19564。

## 8-bit 搞笑音效包（2026-09-13 上午，c5d5e94 已合 main）
- 10 个音效全部 **ffmpeg aevalsrc 合成**（零版权，走用户验证过的正确路线，不用音乐 API）：
  arcade_start/blip/score/over（街机）、npc_open/blip/done（对话）、boing（袋鼠）、moo_8bit（牛来）、exit_fanfare（通关）。
- **ffmpeg aevalsrc 两个坑**：①表达式不支持 C 风格三元 `?:`，要用 `if(gt(x),1,-1)`；②表达式含逗号必须用单引号包住（`aevalsrc='EXPR':s=44100`），否则逗号被 filtergraph 当 filter 分隔符。生成脚本 `.workbuddy/gen_sfx.py` 可复用改参重生成。
- 新增 `scripts/pixel_sfx.gd`：统一播放口，play("cue")；**缺失静默跳过**（缓存 false 标记防重复警告，`_cache.get()` 返回 int 赋给 AudioStream 变量会报类型错——要用 `Variant` 接再 `is AudioStream` 判断）。
- 接线：arcade_snake/tetris/breakout（reset→start、得分→score/blip、死→over、破关→exit_fanfare）；archive_entrance_npc（开聊→play_npc_greeting 彩蛋：牛来=moo_8bit、袋鼠=boing，其余=npc_open；台词→npc_blip；道别→npc_done）。
- 设计原则：梗味靠音色+时机（8-bit 方波+滑音+颤音），不扒热梗原声（版权风险，参赛安全）。
- 回归全绿：check_entrance_npc / check_minigames / check_interaction_map 零 SCRIPT ERROR，真窗口冒烟 exit 0。main = feat = c5d5e94。


---

## 更名奶娃之旅 + 影院换片 + 删多余音效 + 视觉物烘焙框架（完成，commit 3ae45b2，feat+main 同步）

1. **项目更名**：project.godot config/name + assembly_name、export_presets product_name、title_card.gd TITLE 全部统一为「奶娃之旅」（用户曾手改为「奶娃挖坟」，已归位）。
2. **影院视频错了的根因**：cinema_room.gd 的 FEATURE_MP4 指向 cinema_feature.mp4——**该文件从来不在磁盘上**（git 里有、工作区没有），load 失败静默回落 clip_rooftop.ogv（展板的天台片段）→ 用户看到错片。**Godot 4.6.3 实测不支持 mp4**（No loader found for resource）。方案：桌面《影院视频.mp4》（HEVC 1246x576 27.2s）→ ffmpeg 转 Theora ogv（960 宽 q6 intra -g 1，14.3MB，全流解码自检 CLEAN）→ assets/video/cinema_feature.ogv，FEATURE 常量改指 ogv。播放逻辑本就完整播放+finished 循环重播，无截短。
3. **开场多余音效**：mitosis_act.gd swell 段第一帧播的 split.wav——音画错位（离弹开 1.5s），听感是「嘎嘎提拉晒」对话前凭空一声。已删（含 SFX_SPLIT 常量与 _split_played）。BANTER 四句时序不动。
4. **程序化视觉物烘焙框架（新增）**：scripts/visual_baker.gd（class_name VisualBaker：try_mount 两级兜底——编辑器挂载优先、tscn 文件加载兜底；rebuild_all；_save_product 里 pack 前必须 _set_owner_recursive 补 owner）+ bake_visuals.gd（无头重烘入口）+ editor_bake_visuals.gd（@tool EditorScript：编辑器打开 main.tscn → Ctrl+Shift+X 一键重烘并挂进 ArchiveArchitecture 保存）。第一期烘焙 atmosphere（FX 氛围层）+ posters（7 张霓虹海报，85 节点）。生成器入口加 force 参数绕过检测——**坑：烘焙 tscn 已存在时 setup 不带 force 会走 try_mount 命中分支，烘出空壳覆盖原产物**。_panel 的 add_child 必须 (node, true) 强制可读名，否则 4 条 NeonEdge 只有 1 条有名字，_adopt_baked 按 NeonEdge* 收集闪烁会漏。posters 烘焙后闪烁动画由 _adopt_baked 接回（收集烘焙实例里的 NeonEdge）。archive_fx.gd 修为 FX 双来源兼容（FX / BakedVisuals_atmosphere / 自建兜底）。
5. **⚠️ 新坑：--log-file 与 --script 组合段错误**：headless 下 godot --script xxx.gd --log-file xxx.log **必崩**（exit 139，log 文件都不创建），去掉 --log-file 用 stdout 重定向完全正常。今天 check_opening「假崩溃」折腾半天的元凶，与代码无关。以后无头回归一律 stdout 重定向。另外 OGV probe 证明 headless 下 stdout 可抓（GUI exe 才抓不到）。
6. 摄像头清晰度恢复由并行会话完成（640x480 + q75 已进库）；上轮我只改了 q60 注释那半截。

## 2026-09-13 批次 C：更名"奶娃那咋了" + 标题字重设计（续）
- project.godot / export_presets.cfg：奶娃之旅 → 奶娃那咋了（config/name + assembly_name + product_name）。
- title_card.gd：4 字改 5 字 ["奶","娃","那","咋","了"]，通用两段式排版（PAIR_GAP=64 断句缝 + IN_GAP=24 组内距，CELL 收窄 176 防溢出，总宽 1012 居中两侧各留 134px）；新增 drop_x 让中央奶滴对齐"娃|那"缝；副标题"网 络 梗 记 忆 档 案 馆"。
- README.md：标题与描述改为《奶娃那咋了》。
- 回归：check_opening 全 PASS（stdout 重定向，无 --log-file）。待提交推送。

## 2026-09-13 批次 D：真正删掉"嘎嘎提拉晒"前多出的音效（第 2 轮）
- 用户反馈 split.wav 删除后仍有杂音。排查：wav 波形分析（各素材均单句无内嵌杂音）+ 无头音效时间线探针（新工具 check_opening_sound.gd，已入库）+ 全项目发声点盘点 + 导出包 build/NaivaArchive.exe 确认是当天 08:03 新导的（排除旧包）。
- 用"按游戏真实时序混对照音频"让用户听音辨认 → 锁定是 **voice_andy_2（对话+1.10s 安迪追问句，紧贴 gaga 前 0.09s）**。
- 修复：BANTER 4 句改 3 句 mother 0.15 → gaga 1.10 → andy 2.55，BANTER_TIME 5.20→3.90，删 SFX_ANDY_2/VOICES.andy_2（素材文件保留）。check_opening 全 PASS。commit 3252362 已推送双分支。
- **⚠️ ref 被删第 11 次，且升级为"实时删"**：commit 成功后 push 报 src refspec does not match any；update-ref 写回后几秒内又被抹（连 refs/heads/feat/ 目录都没了）。**有效抢救套路 = python 写 ref 后同一进程内立即 push**（写完到推送间隔 <1s 就能成功）。
- 教训：诊断"多出来的音效"类问题，别猜——混一份对照音频让用户听音辨认，一次锁定。另外 bash shim 连 cat 都没了，文件追加一律用 Edit/python，别用 heredoc+cat。

## 2026-09-13 批次 E：迁移队友项目（迁移比对 + 合并实施 + 提交，完成，最终 main=feat=31828d40）
- 比对报告：`C:\Users\wb198\桌面\迁移比对报告.md`（78 相同 / 71 双向分叉 / 259 队友新增 / 100 当前独有）。策略（用户确认）：海报/文案"两套都要"，搞笑道具进主场景。
- 迁入内容：① 动画版模型 yellow_character_animated.glb（AnimationPlayer idle/walk/run/jump/fall/land，main.gd 优先切动画、无则回退程序化摆骨，player.tscn 改引用）；② 科幻贴图地板/墙 + 3 货架 3 储物柜（FBX 缺 13 贴图，11 张从队友 textures/props 补拷，2 张连队友也没有维持原状）；③ 搞笑道具 funny_props.tscn 进主场景（华强买瓜四段式：问瓜→BGM+劈瓜→你劈我瓜是吧→杀人啦 + 知音 wav）；④ 梗问答 quiz（archive_interaction.gd question_data 三题 + 全屏失真 shader overlay，每种记忆小玩法成功后 _begin_quiz()，答对才 _solve）；⑤ 走廊显示屏 archive_video_screen.gd（corridor_screen.ogv 32MB，按 E 播放 Esc 退出，播放冻结移动，router 仲裁）；⑥ 真图轮播相框 archive_photo_frames.gd（复用队友 DISPLAY_SHADER，挂背墙第二排避开大屏）。
- **check_archive covered_2024 断言修复**：pause 窗口超时会 _clear_active()（错过可重试），正确流程 = 窗口内再按 E 才进 quiz。check 流程改为：interact→超时→assert nearby→interact(重新 pause)→interact(窗口内)→assert quiz→选 2→solved。
- **⚠️ ref 被删第 12 次（实时删）**：commit 成功后 rev-parse HEAD 报 ambiguous。抢救升级：`git fsck --no-reflogs --lost-found` 捞 dangling commit（取 committer date 最新）→ 写 ref → 同进程立即 push（全长 sha 作 src）。
- **⚠️ 远端 main 有队友并行 commit**（e8a2024 手势确定+传送返回、7e145d3 传送手势增强+4 张 SVG 海报，GitHubDesktop 推的）→ push main 报 behind its remote。处理：fetch → 确认改动区域与我们的不重叠 → merge（干净无冲突）→ 三个自检回归（check_entrance_npc/check_opening/check_archive 全 PASS）→ 推送。**别强推**。
- 入库 257 文件；排除：临时脚本/日志/对照音频。tasks #16~#21 全部完成。

## 2026-09-13 批次 F：NPC 对话实录语音 + 梗问答声反馈
- **入口 NPC 对话语音**（用户微信发的两个 m4a，ffmpeg 转 16bit 单声道 wav）：
  - 牛来：`voice_niulai.wav` 8.0s（`-t 8` 裁前 8 秒，源 2:25），对话中 **LOOP_FORWARD 循环**（运行时设 `wav.loop_mode`，16bit 单声道 loop_end = data.size()/2）。
  - 美团袋鼠：`voice_kangaroo.wav` 20.3s 全量（《你的胆子真是肥嘟嘟的》），播完即止。
  - archive_entrance_npc.gd：NPCS 表加 `voice`/`voice_loop` 字段；全组共用一个 `voice_player`；`_interact` 开聊即播、`_end_talk`/`set_active(false)` 停；**新增"走开即散"**——`_process` 里 `_distance(active_index) > TALK_RANGE+0.6` 自动结束对话+停语音。
  - **坑：headless 下 AudioStreamPlayer.play() 后 `playing` 需要节点过一帧才生效**——check_entrance_npc 在 `world._ready()` 后必须 `await process_frame`，否则播放断言假失败（但 stream 装载断言同帧就有效）。
- **梗问答声反馈**（用户上一条需求）：答对 → `main.play_frog_laugh()`（laugh.wav + 2.2s 程序化大笑姿态：后仰 -0.30+sin(t*26)*0.09 抽动，apply_locomotion_pose 最前面优先分支，压过 AnimationPlayer 与摆骨骼）；答错 → `archive_interaction.quiz_voice` 播 `voice_andy_2.wav`（"安迪？"，开场删下来的安迪追问句废物利用）。
- 回归：check_entrance_npc（67 项）/ check_archive / check_opening 全 PASS。

