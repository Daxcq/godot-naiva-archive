# 2026-09-12

## 奶蛙：遗忘档案馆 — 接入 WorkBuddy MCP（完成）

**项目概况（扫描结论）**
- Godot 4.6 + GDScript，`gl_compatibility` 渲染，1280×720，主场景 `res://main.tscn`。
- 项目名"奶蛙：遗忘档案馆"，霓虹赛博朋克风的程序化几何档案馆。git 已初始化（origin: Daxcq/godot-naiva-archive），`.gitignore` 只忽略 `.godot/`。
- 结构：`scenes/`（6 个场景）、`scripts/`（54 个 .gd）、`assets/audio`（wav 音效+语音）、`assets/character`（glb + preview.png）、`addons/godot_mcp/`。
- 运行时链路：WASD/方向键移动 + 空格跳；`visual_recognition.gd` 收 6401 UDP 手部状态驱动角色，`vision_preview.gd` 收 6402 UDP 摄像头帧，失联 0.35s 回落鼠标模式。

**本次做的事**
1. 扫描项目结构 + 通读 `main.tscn` / `project.godot` / README / 全部 addons 源码。
2. 发现 `addons/godot_mcp/` 插件写好了但**从未在 `project.godot` 里启用** —— 补上 `[editor_plugins] enabled=PackedStringArray("res://addons/godot_mcp/plugin.cfg")`。
3. 修 `plugin.gd:_exit_tree()` 的 bug：原来用 `get_node("MCPPanel")` 硬编码路径找面板，节点名实际是 `@Panel@14`，导致禁用插件时刷 `ERROR: Node not found: "MCPPanel"` + `Parameter "p_control" is null`。改为保存 instances 引用 `panel`，`is_instance_valid` 校验后 `remove_control_from_bottom_panel(panel)` + `queue_free()`。验证后启用/禁用两侧日志都干净。
4. 新写 `addons/godot_mcp/mcp_server.py`（纯标准库）：标准 MCP stdio server，把 MCP `tools/call` 翻译成插件 TCP 协议（`{"type","params"}` → 一行 JSON 返回），注册 24 个 `godot_*` 工具。凭据走 env：`GODOT_MCP_HOST/PORT/TIMEOUT/PROJECT_PATH`。编辑器不在线时返回中文指引而非崩溃。
5. 写入 `~/.workbuddy/mcp.json`，新增 `godotMCP`（保留原有 `unityMCP`）。command 用 `C:\Program Files\python\python.exe`（系统 3.8.6，路径稳定）。
6. README 补"编辑器 MCP 接入"章节：启用方式、端口占用说明、配置文件、使用顺序。

**关键结论 / 坑**
- 端口不冲突：插件 TCP **6400 只在 Godot 编辑器进程内监听**；运行时视觉链路用 UDP 6401/6402，游戏运行时不占 6400。不要误以为是资源冲突。
- 插件确实能正常加载：`--headless --editor --path . --quit-after 30` 会打印 `Godot MCP Plugin activated` / `listening on port 6400`。注意 `--quit-after` 是**帧数不是秒**，所以脚本里靠 OS-level 轮询 6400 端口会误判"插件没启用"——验证要以编辑器 stdout 为准。
- **使用前提：必须先让 Godot 编辑器打开本工程并保持运行**，MCP 才有连接对象。仅启动编辑器不够——插件要启用。
- 用户当前已有一个 Godot 编辑器进程（pid 10144，`E:\Godot\Godot_v4.6.3-stable_win64.exe`）开着，走 recovery 模式，早于本次改动启动 → 需要重开或重新加载项目。

**待用户操作**
- 重启 Godot 编辑器（或 项目→重新加载当前项目），确认底部出现 MCP 面板。
- 在连接器管理页右上角自定义连接器入口对 `godotMCP` 点"信任"，然后重载 WorkBuddy 让工具生效。

---

## 老项目 → 新项目 功能迁移（完成）

**起因**：俊博反馈老项目 `Desktop/shijueshibie-jikesong-S2` 有几样功能新项目没有：①开屏奶蛙位置 ②奶蛙大笑效果 ③传送进游戏后移动失效 ④摄像头随游戏启停。

**关键发现（重新定位了问题性质）**：比对老/新项目，**27 个 .gd + 6 个 .tscn + main.tscn 全部逐字节相同**。所以"功能没了"不是代码丢失，而是**配置 / 打包层面的回归**。这个结论决定了排查方向。

### ①+② 开屏位置 & 大笑效果 → 代码本来就对，是测试脚本在骗人
- `opening_director.gd` 的 `FROG_PERCH := Vector3(-3.5, 2.585, -0.35)`、侧倾 -0.16；`mitosis_act.gd` 大笑段 `body.rotation.x = -amount * 0.34`（amount=0.30 → 后仰 -0.104）——两处代码完好。
- 原 `check_opening.gd` 误报：它调 `enter("sleep")`，但真实状态机没有 `sleep` 这个阶段（正确名是 `idle`），所以演出根本没推进就"失败"了。
- 重写 `check_opening.gd` 为聚焦断言。**重要坑**：`--script`（SceneTree）模式下节点 `_process` **不会自动跑**，必须显式驱动 `terminal._process(dt)` + `update(dt)`，`boot`/`pull` 这类点击门控阶段还要注入合成 confirm。

### ③ 传送后移动失效 → 唯一的真·代码级 bug
- 根因：`project.godot` 的 `[input]` 里每个 `InputEventKey` 都被序列化带上 **`"device":16`**。带 device 限制的事件只匹配 `device=16` 的合成事件，而**真实硬件键盘发的是 `device=0`**，所以键盘全死。
- 实验证明（一次性 `diag_input.gd`）：合成事件 device=0 → `move_forward` **false**；device=16 → true。
- 修复：重写 `[input]` 段，只留最小 `Object(InputEventKey,"physical_keycode":N)`，并在文件里留中文注释警告别再让编辑器加回 `device` 字段。
- 新增 `scripts/check_input.gd`：6 个动作全 `device=0` 且对真实按键合成事件有响应（WASD/方向键/空格/E）。

### ④ 摄像头随游戏启停 → 打包缺口
- 根因：`visual_recognition.gd` 里 `res://../scripts/mediapipe_camera_bridge.py` 解析到 `Desktop/scripts/`，而依赖实际只存在于老项目里，新项目根本没有 → 永远启动失败、静默回落鼠标模式。
- 修复：把 `mediapipe_camera_bridge.py` / `godot_camera_bridge.py` / `hand_landmarker.task`(7.8MB) / `.vision-venv` 一并迁进新项目 `vision/`（工程内自包含）。
- 重写 `visual_recognition.gd` 生命周期：`_start_bridge()` 用 `OS.create_process` 起子进程；`_shutdown_bridge()` 退出时 `taskkill /PID /T /F` 递归回收；`_process` 里带 6s 冷却的自动重启。
- `mediapipe_camera_bridge.py` 顺手修了几处：模型路径改指同目录、加 SIGINT/SIGTERM 处理 + `_running` 标志、把原来那个 `type('P',(),{...})()` 的骚操作换成正经 `_P` 类、`finally` 里释放摄像头。
- `.gitignore` 补 `vision/.vision-venv/`（约 400MB 不入库）、`__pycache__/`、`*.pyc`。

**验证**：`check_input` / `check_opening` / `check_archive` / `check_minigames` 四个无头自检**全 PASS**；摄像头桥接单独验过双 UDP 流 + SIGTERM 干净退出，在 Godot 内验过 `create_process -> pid=4488` 且退出后 `Get-CimInstance` 确认无残留进程。

**已提交**：`dc63300 fix: 修复移动输入失效(device:16) + 摄像头桥接自动启停迁移 + 开屏演出断言`

**教训**：`--script` 模式不驱动 `_process`；`--quit-after` 是帧不是秒；Windows 下 WMIC 废弃，进程枚举改用 PowerShell `Get-CimInstance Win32_Process`。
